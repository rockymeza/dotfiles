#!/usr/bin/env bash
if [[ -f fbmvc ]]; then
  ./fbmvc $@
else
  while [ 1 ]; do
    if [[ "$PWD" == "/" ]]; then
      echo 'this is not an fbmvc directory'
      exit 1
    fi

    cd ..
    if [[ -f fbmvc ]]; then
      ./fbmvc $@
      exit 0
    fi
  done
fi
